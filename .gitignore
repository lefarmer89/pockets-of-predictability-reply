# Build artifacts: regenerated by runAll(default_config()).
# .mat files ship via the Dropbox archive (see MAT_ARCHIVE.md), not git.
results/**/*.mat
results/**/*.eps
results/**/*.png
output/figures/*.eps
output/figures/*.png
output/tables/*.tex
output/logs/

# Smoke-pipeline scratch directory (created by tests/smoke_pipeline.m).
results_smoke/

# Online-only data placeholders (~72 GB on Dropbox; do not commit).
data/csv_sims/

# Large data inputs (ship via Dropbox archive, not git).
data/pcPredictor.mat

# Session ephemera
*.log
*.status
*.tmp.*
*.bak_*
diary
*.staletopK*
*.preRefactorBackup_*

# MATLAB
*.asv
*.m~
slprj/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local working directory (not part of the public package).
letter/

# MAT-archive scratch (created when zipping results/ for distribution).
results.zip
results_*.zip
